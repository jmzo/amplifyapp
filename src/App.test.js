import React from 'react';
import { render } from '@testing-library/react';
import App from './App';

test('renders element', () => {
  // const { getByText } = render(<App />);
  // const linkElement = getByText('We now have Auth!');
  // expect(linkElement).toBeInTheDocument();
});
